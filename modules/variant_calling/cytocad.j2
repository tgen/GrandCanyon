{% macro cytocad(sample) %}

{%- set bam %}{{ sample.gltype }}/alignment/minimap2/{{ sample.name }}/{{ sample.name }}.minimap2.cram{% endset %}
{%- set temp_dir %}temp/{{ sample.gltype }}/constitutional_copy_number/cytocad/{{ sample.name }}_minimap2{% endset %}
{%- set results_dir %}{{ sample.gltype }}/constitutional_copy_number/cytocad/{{ sample.name }}_minimap2{% endset %}
{%- set all_vcf %}{{ results_dir }}/{{ sample.name }}.minimap2.cytocad.all.vcf.gz{% endset %}
{%- set pass_vcf %}{{ results_dir }}/{{ sample.name }}.minimap2.cytocad.pass.vcf.gz{% endset %}

- name: cytocad_{{ sample.name }}_minimap2
  input:
    - {{ bam }}
    - {{ bam }}.bai
    - {{ constants.grandcanyon.reference_fasta }}
  output:
    - {{ all_vcf }}
  cpus: 16
  mem: 32G
  walltime: "24:00:00"
  queue_preset: "DEFAULT"
  container: {{ constants.tools.cytocad.container }}
  cmd: |
    set -eu
    set -o pipefail

    cytocad \
      {{ bam }} \
      {{ results_dir }} \
      -b hg38 \
      -f png \
      -i 50000 \
      -j 10 \
      -r 10 \
      -p 500 \
      -s 0.25 \
      --add_plots

{% endmacro %}
