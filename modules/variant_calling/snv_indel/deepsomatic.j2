{%- macro deepsomatic(pair, aligner) %}

{%- set normal_bam %}temp/{{ pair.normal.gltype }}/alignment/{{ aligner }}/{{ pair.normal.name }}/{{ pair.normal.name }}.{{ aligner }}.bam{% endset %}
{%- set tumor_bam %}temp/{{ pair.tumor.gltype }}/alignment/{{ aligner }}/{{ pair.tumor.name }}/{{ pair.tumor.name }}.{{ aligner }}.bam{% endset %}
{%- set temp_dir %}temp/{{ pair.gltype }}/somatic_variant_calls/deepsomatic/{{ pair.name }}_{{ aligner }}{% endset %}
{%- set results_dir %}{{ pair.gltype }}/somatic_variant_calls/deepsomatic/{{ pair.name }}_{{ aligner }}{% endset %}
{%- set all_vcf %}{{ results_dir }}/{{ pair.name }}.{{ aligner }}.deepsomatic.all.vcf.gz{% endset %}
{%- set all_gvcf %}{{ results_dir }}/{{ pair.name }}.{{ aligner }}.deepsomatic.all.g.vcf.gz{% endset %}
{%- set pass_vcf %}{{ results_dir }}/{{ pair.name }}.{{ aligner }}.deepsomatic.pass.vcf.gz{% endset %}

{% if pair.tumor.rgpl not in ['PACBIO', 'ILLUMINA'] %}

{% else %}
- name: deepsomatic_{{ pair.name }}_{{ aligner }}
  tags: [{{ pair.gltype }}, somatic, snp_indel_caller, deepsomatic, {{ pair.name }}]
  input:
    - {{ tumor_bam }}
    - {{ tumor_bam }}.bai
    - {{ normal_bam }}
    - {{ normal_bam }}.bai
    - {{ constants.grandcanyon[reference].reference_fasta }}
  output:
    - {{ all_vcf }}
    - {{ all_gvcf }}
  cpus: 20
  mem: 120G
  walltime: "12:00:00"
  queue_preset: "DEEPVARIANT"
  container: {{ constants.tools.deepsomatic.container }}
  cmd: |
    set -eu
    set -o pipefail

    rm -r {{ temp_dir }} || true
    mkdir -p {{ temp_dir }}
    mkdir -p {{ results_dir }}
    export TMPDIR={{ temp_dir }}/intermediate_results_dir

    run_deepsomatic \
      {% if pair.tumor.rgpl in ('PACBIO') %}
      --model_type PACBIO \
      {% else %}
      --model_type WGS \
      {% endif %}
      --ref {{ constants.grandcanyon[reference].reference_fasta }} \
      --reads_tumor {{ tumor_bam }} \
      --reads_normal {{ normal_bam }} \
      --output_vcf {{ all_vcf }} \
      --output_gvcf {{ all_gvcf }} \
      --sample_name_tumor {{ pair.tumor.name }} \
      --sample_name_normal {{ pair.normal.name }} \
      --num_shards 20 \
      --novcf_stats_report \
      --logging_dir {{ temp_dir }}/logs \
      --intermediate_results_dir {{ temp_dir }}/intermediate_results_dir


- name: deepsomatic_filter_variants_{{ pair.name }}_{{ aligner }}
  tags: [{{ pair.gltype }}, somatic, snp_indel_caller, deepsomatic, {{ pair.name }}]
  input:
    - {{ all_vcf }}
  output:
    - {{ all_vcf }}.tbi
    - {{ pass_vcf }}
    - {{ pass_vcf }}.tbi
  cpus: 1
  mem: 4G
  walltime: "8:00:00"
  queue_preset: "DEFAULT"
  container: {{ constants.tools.bcftools.container }}
  cmd: |
    set -eu
    set -o pipefail

    bcftools index --tbi --force "{{ all_vcf }}"

    bcftools filter \
      --include 'FILTER == "PASS"' \
      --output-type z \
      --output {{ pass_vcf }} \
      --write-index \
      {{ all_vcf }}

{% endif %}

{% endmacro %}
